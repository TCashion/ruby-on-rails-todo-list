<h1>Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li>
      <%= link_to task.title, task %> | <%= task.complete ? "√" : "X" %>
    </li>
  <% end %>
</ul>

<div>
  <%= link_to "New Task", new_task_path %>
</div>
